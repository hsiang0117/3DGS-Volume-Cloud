# 3DGS-Volume-Cloud — Python dependencies (Python 3.12, CUDA 12.8).
# Install:  pip install -r requirements.txt
#
# torch/vision are the CUDA 12.8 wheels from the PyTorch index (PyPI only has CPU
# builds). The three submodules are local CUDA extensions and must be compiled
# against the installed torch — reinstall them after changing torch or the CUDA
# kernels (analytic-tau / record_front_tau / lightpass-backward).

--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128

numpy==2.4.4
pillow==12.1.1
opencv-python==4.13.0.92
plyfile==1.1.3
tqdm==4.67.3
matplotlib==3.10.9
viser==1.0.27          # interactive viewer
lpips==0.1.4           # test-set LPIPS metric
tensorboard            # optional: train.py logs to it if present

# Local CUDA extensions (this fork's rasterizer + kNN + fused SSIM). Need a CUDA
# toolchain to build; see README. The viewer/training won't import without them.
./submodules/diff-gaussian-rasterization
./submodules/simple-knn
./submodules/fused-ssim
